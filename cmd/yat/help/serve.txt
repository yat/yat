Serve runs a Yat server.

Usage:

  yat serve

Flags:

  -bind localhost:25120
      The address the server should bind.

  -allow-all
      If this flag is set, the server allows all operations.
      Any configured rules are parsed, but not enforced.

The -tls-dir flag or YAT_TLS_DIR environment variable must be set.
