Subscribe prints a stream of messages as JSON lines.

Usage:

  yat subscribe PATH

Flags:

  -limit, -n 0
      Stop after n deliveries.

  -duration, -d 0s
      Stop after some amount of time.

  -raw
      Print raw message data.
