Publish publishes a message.

Usage:

  yat publish PATH

Flags:

  -file, -f /dev/stdin
      Read message data from this file.
      This flag is ignored if -empty is set.

  -empty, -e
      Publish a message with no data.

  -inbox, -i PATH
      Set the message inbox.
