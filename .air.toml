[build]
cmd = "bin/build"
entrypoint = ["go", "tool", "logfmt", "./yat", "server", "-log-level", "debug", "-tls-dir", "tmp/tls"]
include_ext = ["go"]
include_file = ["go.mod", "go.sum"]
send_interrupt = true
kill_delay = "500ms"

[log]
silent = true
