#!/bin/sh
# serve runs the dev server.
# usage: bin/serve [args]

set -eu

bin/build

# generate a local dev config on demand
[ -f tmp/local/README ] || ./yat local tmp/local

exec go tool logfmt -x time \
  ./yat serve -log-level debug -local tmp/local "$@"
