#!/bin/sh
# build and run yat serve in dev

set -eu

bin/build

export YAT_TLS_CA_FILE=tmp/dev/tls/ca.crt
export YAT_TLS_CERT_FILE=tmp/dev/tls/server.crt
export YAT_TLS_KEY_FILE=tmp/dev/tls/server.key

exec go tool logfmt -x time ./yat serve "$@"
