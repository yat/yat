#!/bin/sh
# run the local dev server

exec go tool logfmt ./yat serve \
  -log-level debug \
  -tls-cert-file tmp/seed/tls.crt \
  -tls-key-file tmp/seed/tls.key \
  -tls-ca-file tmp/seed/ca.crt \
  -config tmp/seed/rules.yaml
