#!/bin/sh
# serve runs the dev server.
# usage: bin/serve [args]

set -eu

bin/build

exec go tool logfmt -x time \
  ./yat serve \
    -auth-rules testdata/auth/local.yaml \
    -local-tls tmp/tls \
    -log-level debug "$@"
