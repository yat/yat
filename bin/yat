#!/bin/sh
# run yat client commands

set -eu

bin/build

unset YAT_TOKEN
unset YAT_TOKEN_FILE

export YAT_SERVER=localhost:25120
export YAT_TLS_CERT_FILE=tmp/tls/tls.crt
export YAT_TLS_KEY_FILE=tmp/tls/tls.key
export YAT_TLS_CA_FILE=tmp/tls/ca.crt

exec ./yat "$@"
